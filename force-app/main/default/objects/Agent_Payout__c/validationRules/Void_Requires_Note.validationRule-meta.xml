<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Void_Requires_Note</fullName>
    <active>true</active>
    <description>Voiding a payout releases its lines back to unpaid, so the reason has to be written down.</description>
    <errorConditionFormula>AND(
  ISPICKVAL(Status__c, &quot;Voided&quot;),
  ISBLANK(Notes__c)
)</errorConditionFormula>
    <errorDisplayField>Notes__c</errorDisplayField>
    <errorMessage>Enter a note explaining why this payout is being voided.</errorMessage>
</ValidationRule>
